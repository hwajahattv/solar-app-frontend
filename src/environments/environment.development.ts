export const environment = {
  production: false,
  /** Local gateway. Override by editing this file or pointing ng serve at a remote. */
  apiBaseUrl: 'https://solar-app-ochre.vercel.app/api/v1',
    // apiBaseUrl: '/api/v1',

  defaultRefreshIntervalMs: 5000,
};
