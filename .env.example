# Absolute Knox Solar Gateway API base (used at build time by scripts/write-env.mjs)
API_BASE_URL=https://your-gateway.example.com/api/v1

# Vercel Edge PIN gate (set in the Vercel project env; never commit real values)
APP_PIN=123456
PIN_SECRET=replace-with-a-long-random-string
